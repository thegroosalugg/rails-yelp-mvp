<h1>Restaurants Index</h1>
<p>Got Munchies? Got Money? Go Get</p>
<p class="small">"Disclaimer: if you do not have money then go home"</p>

<div class="reviews-container overflow-auto" style="max-height: 300px;">
  <% @restaurants.each do |restaurant| %>
    <div class="card shadow my-1 text-shadow-bold" style="border-radius: 10px;">
      <div class="card-body py-1" style="background-color: #4682A9; border-radius: 10px;">
        <h6 class="my-1"><%= link_to restaurant.name, restaurant_path(restaurant), class: "text-decoration-none" %></h6>
      </div>
    </div>
  <% end %>
</div>

<%= button_to "New restaurant", new_restaurant_path, method: :get, class: "btn btn-light mt-3" %>
